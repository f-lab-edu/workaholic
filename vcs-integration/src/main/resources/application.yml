server:
  port: 9000

integration:
  name: workaholic
  base-url: ${BASE_URL}

spring:
  application:
    name: workaholic-vcs
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    listener:
      direct:
        prefetch: 10
      simple:
        prefetch: 10